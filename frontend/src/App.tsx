import './App.css'

// Placeholder shell. The real UI (symbol search and intraday daily aggregates)
// and its consumption of the shared contracts land in a later commit.
export default function App() {
  return (
    <main className="app">
      <h1>Stock Market MVP</h1>
      <p>Frontend scaffold is ready.</p>
    </main>
  )
}
